[variables]
PORT = "3000"

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "ls -la /app && ls -la /app/dist && caddy run --config Caddyfile --adapter caddyfile 2>&1"

