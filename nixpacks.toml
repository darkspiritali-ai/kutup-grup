providers = ["node"]

[variables]
PORT = "3000"
NIXPACKS_SPA_CADDY = "false"

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "PORT=3000 node server.js"
