[variables]
PORT = "3000"
NODE_ENV = "development"
NPM_CONFIG_PRODUCTION = "false"

[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'Skipping build, using pre-built dist folder'"]

[start]
cmd = "node server.js"
