# Project Rules

- **Automatic Git Push**: After making any changes or code modifications, always automatically stage the changes, commit them with a descriptive message, and execute `git push` to push them to the remote repository.
